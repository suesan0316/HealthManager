<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:extention="clr-namespace:HealthManager.Common.Extention;assembly=HealthManager"
             x:Class="HealthManager.View.BodyImageView">
    <ContentPage.Content>
        <StackLayout>
            <ScrollView Orientation="Horizontal">
                <extention:BindableStackLayout Items="{Binding BodyImageContents}"  Orientation="Horizontal" />
            </ScrollView>
            <Button Text="戻る" Command="{Binding BackHomeCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>