<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:HealthManager.ViewModel"
             xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
             x:Class="HealthManager.View.RegistBodyImageView">
    <ContentPage.BindingContext>
        <vm:RegistBodyImageViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <controls:ImageButton Text="{Binding TakeImageCameraLabel}"  Command="{Binding TakeImageCameraCommand}" Orientation="ImageToLeft"  Source="camera.png" ImageHeightRequest="200" ImageWidthRequest="200" HeightRequest="120"></controls:ImageButton>
                <controls:ImageButton Text="{Binding TakeImageLibraryLabel}" Command="{Binding TakeImageLibraryaCommand}" Orientation="ImageToLeft"  Source="library.png" ImageHeightRequest="200" ImageWidthRequest="200" HeightRequest="120"></controls:ImageButton>
                <Image Source="{Binding BodyImage}"></Image>
                <Button Text="{Binding RegistButtonLabel}" IsVisible="{Binding RegistButtonIsVisible}" Command="{Binding RegistBodyImageCommand}" Style="{StaticResource NormalButtonStyle}"></Button>
                <Button Text="{Binding CancelButtonLabel}" Command="{Binding BackHomeCommand}" Style="{StaticResource NormalButtonStyle}"></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>